var assert = require('assert')

describe('Example Group 1', function () {
  describe('Example Task 1', function () {
    it('Should check the positive numbers equality.', function () {
      assert.equal(1, 1)
    })

    it('Should check the negative numbers equality.', function () {
      assert.equal(-1, -1)
    })
  })
})
